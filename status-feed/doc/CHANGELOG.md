# Changelog

## 0.1.0

- First release. Reference plugin: an own-service plugin whose Python
  dependencies are installed into a per-plugin virtual environment from wheels
  baked into the package, with no pip ever running on the printer.
- Experimental: serves as a worked example for plugin authors.
